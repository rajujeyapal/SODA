name: Scan for data quality
 
on: pull_request
 
jobs:
  soda_scan:
    runs-on: ubuntu-latest
    name: Run Soda Scan
    steps:
      - name: Checkout
        uses: actions/checkout@v3
 
      - name: Perform Soda Scan
        uses: sodadata/soda-github-action@v1.0.2
        with:
          soda_library_version: v1.5.5
          data_source: snowflake
          configuration: ./configuration.yml
          checks: ./checks.yml
        env:
          SODA_CLOUD_API_KEY: 0c1708b7-30fd-41c0-8409-8f531fd58eff
          SODA_CLOUD_API_SECRET: ETRvLV28YJstJYGPtBLWSYQW2bf14Y_Tup7lv44x8b0IUjWOGpdRsQ
          SNOWFLAKE_USERNAME: RAJU
          SNOWFLAKE_PASSWORD: Dhiyaguru@465577
