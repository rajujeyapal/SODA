name: Scan for data quality
 
on: pull_request
 
jobs:
  soda_scan:
    runs-on: ubuntu-latest
    name: Run Soda Scan
    steps:
      - name: Checkout
        uses: actions/checkout@v3
 
      - name: Perform Soda Scan
        uses: sodadata/soda-github-action@v1.0.2
        with:
          soda_library_version: v1.5.5
          data_source: snowflake
          configuration: ./configuration.yml
          checks: ./checks.yml
        env:
          SODA_CLOUD_API_KEY: 75f02b82-989f-475a-ae79-51cf7b58bad4
          SODA_CLOUD_API_SECRET: 4s8lPrQlmFgTXR-BkKkvR6h6qp0uUCXs0ZAILzPShJCV_rXZ_eGDZg
          SNOWFLAKE_USERNAME: raju
          SNOWFLAKE_PASSWORD: Dhiyaguru@465577
